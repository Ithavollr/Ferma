# Frozen World Pack Example
# Demonstrates a world with always-cold temperature and vanilla everything else

schema_version: 1
id: desert_world
name: "Desert World"
description: "Always-hot temperature (+1.0), vanilla everything else"
type: noise

climate:
  temperature:
    type: constant
    value: 1.0
